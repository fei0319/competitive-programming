#include <bits/stdc++.h>

void solve() {
    int x;
    std::cin >> x;
    std::cout << x * 2 << '\n';
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int t;
    std::cin >> t;

    while (t--) solve();
}
